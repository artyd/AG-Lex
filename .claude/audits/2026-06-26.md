# Daily audit — 2026-06-26

## Run 01 — inaugural (harness landing)

### Branches in flight

| Branch                              | Last activity | Author |
|-------------------------------------|---------------|--------|
| `chore/claude-harness`              | now           | artyd  |
| `main`                              | ~24 h ago     | artyd  |
| `feat/viktoria-clean-workspace`     | ~24 h ago     | artyd  |
| `feat/viktoria-seed-account`        | ~25 h ago     | artyd  |
| `hotfix/codex-bootstrap-background` | ~26 h ago     | artyd  |
| `feat/codex-autoseed-and-md-reader` | ~26 h ago     | artyd  |
| `fix/legislation-fill-window`       | ~27 h ago     | artyd  |
| `feat/legislation-rag-badge-type-filter` | ~29 h ago | artyd  |

Single-author repo at this snapshot. Several merged feature branches
still around — safe to delete locally once verified merged into main.

### Recent merges (last 8 commits on main)

- 5eedbee — Feat/viktoria clean workspace (#62)
- 2ef2bc6 — feat(auth): seed Вікторія Верещагіна partner account on startup (#61)
- 3707fb0 — fix(legislation): run codex bootstrap in a background thread (#60)
- dfbb47d — feat(analysis+legislation): markdown reader replaces PDF viewer (#59)
- c10c9ab — fix(legislation): library fills full window (#58)
- fe60c31 — chore: ignore .claude local settings (botched: `. c l a u d e /`)
- dbdcea0 — Merge feat/legislation-rag-badge-type-filter
- ce35757 — feat(legislation): RAG indicator, source counter, type filter (#57)

### Recent TODO/FIXME deltas

None added in this branch (`chore/claude-harness` is pure harness work).

### Business-critical files touched in this branch

None. This PR only adds `CLAUDE.md`, `.claude/`, and fixes the corrupted
`.claude/` line in `.gitignore`.

### Codex health

Not probed in this run (the audit is observational; the inaugural snapshot
predates harness verification). Recommended first command after merge:

```
python legal_app/scripts/check_codex.py
```

If `vec_ready=false`, the bootstrap thread (PR #60) is still working —
wait a few minutes on a fresh volume.

### Smoke checks

- lint: not yet run (deferred to `/pre-merge` before merging this PR)
- vitest: not yet run
- pytest: not yet run

### Known issues observed

- `.gitignore` had `. c l a u d e /` (literal spaces) — corrupted from a
  prior paste. This PR fixes it to `.claude/settings.local.json`. Logged
  under `.claude/docs/KNOWN_ISSUES.md` 2026-06-26.
- Codex bootstrap inflates startup cost on fresh volumes (RAG falls back
  to FTS-only until embeddings finish). Logged under KNOWN_ISSUES.md.

### Verdict

**GREEN** — main is healthy; this branch only adds documentation and
sub-agents; no critical-file edits.

### Top action

Run `/pre-merge` on `chore/claude-harness` to verify lint+tests still
pass after these additions, then merge. Subsequent runs of `/daily-audit`
will start populating real smoke-check + codex status data.
