---
description: Full preflight before opening a PR (lint, tests, build, e2e, docs check). Wraps the `pre-merge-checklist` skill.
---

Invoke `Skill: pre-merge-checklist`.

Don't push, don't commit, don't open a PR. The user pairs this with
`/draft-pr` once it passes.
