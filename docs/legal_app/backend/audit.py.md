<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/audit.py`

_Python module_

## Overview

Audit log for RBAC + team-management actions (Phase 2.3, spec §5.3).

Append-only. Every role change, permission toggle, invite, or removal lands
here with the actor's user id + name, the canonical action code, a free-form
target string for display, and the timestamp. The Team UI tail-reads this for
the «Аудит-лог» tab.

`actor_name` is denormalized so the log survives a user being deleted.

## Public API

### `init_audit_schema`

```
init_audit_schema(conn: sqlite3.Connection) -> None
```

_No description provided._

_Defined at line 41._

### `log`

```
log(conn: sqlite3.Connection, *, actor: dict | None, action: str, target: str = '', meta: dict | None = None) -> dict
```

Append one audit row. Returns the inserted row as a dict.

_Defined at line 46._

### `list_audit`

```
list_audit(conn: sqlite3.Connection, *, limit: int = 200) -> list[dict]
```

Return audit rows newest first, capped at `limit`.

_Defined at line 75._
