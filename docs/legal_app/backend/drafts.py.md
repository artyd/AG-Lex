<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/drafts.py`

_Python module_

## Overview

Draft documents — custom router with personal / team scoping.

Fix 1 swaps the generic CRUD that Phase 3.3 stood up for a dedicated module
because none of the other 13 workspace entities need per-row authorization.
Rules:

  - List returns rows where `user_id = current_user.id` OR `is_shared = 1`.
    The author column comes from a `JOIN users` so the UI can label team
    drafts with whoever wrote them.
  - Create injects `user_id` from the JWT — never trusts the body.
  - Get / PATCH / DELETE: visible if mine or shared, but mutations are
    rejected unless I'm the author or my role has `manage`.
  - PATCH /{id}/share toggles `is_shared`. Same author-or-manage gate.

The wire shape mirrors the prototype (camelCase `typeId`, `documentMarkdown`,
`createdAt`, `userId`, `isShared`, `authorName`). JSON columns (`params`,
`options`) decode to objects on read and encode on write.

## Public API

### `DraftIn` · class

```
class DraftIn
```

_No description provided._

_Defined at line 41._

### `DraftPatch` · class

```
class DraftPatch
```

_No description provided._

_Defined at line 53._

### `list_drafts`

```
@router.get('')
```

```
list_drafts(user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> list[dict]
```

Personal drafts I authored + team-shared drafts from anyone.

_Defined at line 118._

### `get_draft`

```
@router.get('/{draft_id}')
```

```
get_draft(draft_id: str, user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 132._

### `create_draft`

```
@router.post('', status_code=status.HTTP_201_CREATED)
```

```
create_draft(body: DraftIn, user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 150._

### `update_draft`

```
@router.patch('/{draft_id}')
```

```
update_draft(draft_id: str, body: DraftPatch, user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 181._

### `delete_draft`

```
@router.delete('/{draft_id}', status_code=status.HTTP_204_NO_CONTENT)
```

```
delete_draft(draft_id: str, user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db))
```

_No description provided._

_Defined at line 219._

### `share_draft`

```
@router.patch('/{draft_id}/share')
```

```
share_draft(draft_id: str, user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

Toggle `is_shared`. Author or `manage` only.

Returning the full updated draft is the cheapest way for the UI to
re-render without a follow-up GET.

_Defined at line 235._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_row_to_dict`

```
_row_to_dict(row) -> dict | None
```

_No description provided._

_Defined at line 74._

### `_can_mutate`

```
_can_mutate(draft_row: dict, user: dict, conn: sqlite3.Connection) -> bool
```

Author OR `manage` may edit/delete/share.

_Defined at line 102._

### `_new_id`

```
_new_id() -> str
```

_No description provided._

_Defined at line 109._
