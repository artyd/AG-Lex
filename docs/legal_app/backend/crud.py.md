<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/crud.py`

_Python module_

## Overview

Generic SQL CRUD + FastAPI router factory for Phase 2.2 entities.

Each of the 13 workspace entities follows the same shape: a flat row, a TEXT
primary key, no joins. The `Entity` class captures that pattern once; each
table becomes a 4-line declaration. A single `build_router` then mounts five
standard endpoints, all gated by `current_user`.

Why not Pydantic models per entity:
The columns are evolving (especially in Phases 3.x), the wire shape mirrors
the row 1:1, and FastAPI doesn't need a typed model to JSON-encode a dict.
Tightening schemas is straightforward later; the cost now is rigidity.

## Public API

### `Entity` · class

```
@dataclass
```

```
class Entity
```

Per-table metadata used by the CRUD helpers and the router factory.

_Defined at line 33._

### `Entity.all_columns` · method

```
@property
```

```
all_columns(self) -> tuple[str, ...]
```

_No description provided._

_Defined at line 51._

### `Entity.db_to_wire` · method

```
@property
```

```
db_to_wire(self) -> dict[str, str]
```

_No description provided._

_Defined at line 55._

### `Entity.wire_field` · method

```
wire_field(self, db_col: str) -> str
```

_No description provided._

_Defined at line 58._

### `Entity.db_field` · method

```
db_field(self, wire_col: str) -> str
```

_No description provided._

_Defined at line 61._

### `Entity.new_id` · method

```
new_id(self) -> str
```

_No description provided._

_Defined at line 64._

### `list_rows`

```
list_rows(conn: sqlite3.Connection, entity: Entity) -> list[dict]
```

_No description provided._

_Defined at line 108._

### `get_row`

```
get_row(conn: sqlite3.Connection, entity: Entity, pk_value: str) -> dict | None
```

_No description provided._

_Defined at line 114._

### `insert_row`

```
insert_row(conn: sqlite3.Connection, entity: Entity, payload: dict) -> dict
```

_No description provided._

_Defined at line 123._

### `update_row`

```
update_row(conn: sqlite3.Connection, entity: Entity, pk_value: str, payload: dict) -> dict | None
```

_No description provided._

_Defined at line 139._

### `delete_row`

```
delete_row(conn: sqlite3.Connection, entity: Entity, pk_value: str) -> bool
```

_No description provided._

_Defined at line 153._

### `upsert_many`

```
upsert_many(conn: sqlite3.Connection, entity: Entity, rows: Iterable[dict]) -> int
```

Bulk seed helper: INSERT OR IGNORE so re-running the seed is a no-op.

_Defined at line 162._

### `build_router`

```
build_router(entity: Entity, *, tag: str | None = None, read_capability: str | None = None, write_capability: str | None = None) -> APIRouter
```

Mount GET list / GET one / POST / PATCH / DELETE for one entity.

`read_capability` / `write_capability` add an `rbac.require()` gate on top
of the bare authentication check. Pass `None` (default) to require only
a valid bearer token. Used in Phase 2.3 to gate `invoices` by `billing`.

_Defined at line 193._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_row_to_dict`

```
_row_to_dict(entity: Entity, row: sqlite3.Row | tuple | None) -> dict | None
```

_No description provided._

_Defined at line 72._

### `_wire_to_db`

```
_wire_to_db(entity: Entity, payload: dict) -> dict
```

Translate a wire payload (camelCase, JSON-decoded) to a DB row dict.

Only known columns are kept — extras are silently dropped, which keeps
the frontend free to ship richer objects than the table strictly tracks.

_Defined at line 86._
