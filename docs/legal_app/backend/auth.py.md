<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/auth.py`

_Python module_

## Overview

Authentication for AG Lex — bcrypt + JWT.

Phase 2.1. Three routes (`/api/auth/register`, `/login`, `/me`), one FastAPI
dependency (`current_user`), one startup seed (`seed_test_user`). Sessions are
stateless JWTs — no `sessions` table for the MVP; revocation can land later.

Security choices:
  - bcrypt direct (12 rounds, library default — slow enough that brute force
    is impractical, fast enough not to break login UX). Passlib was dropped
    after Phase 2.1: passlib 1.7.4 misreads bcrypt 4.x+'s `__about__` and
    falls through to a broken codepath. Calling the `bcrypt` module directly
    is the upstream-recommended workaround.
  - JWT HS256 signed with `settings.JWT_SECRET`. Payload: `sub` (user_id),
    `role`, `exp`. 24-hour TTL per Phase 2.1 doc.
  - Single endpoint surface returns generic "Invalid email or password" on
    login failure — no user-enumeration leak via timing or distinct messages.

## Public API

### `RegisterRequest` · class

```
class RegisterRequest
```

_No description provided._

_Defined at line 64._

### `LoginRequest` · class

```
class LoginRequest
```

_No description provided._

_Defined at line 71._

### `UserOut` · class

```
class UserOut
```

_No description provided._

_Defined at line 76._

### `TokenResponse` · class

```
class TokenResponse
```

_No description provided._

_Defined at line 84._

### `hash_password`

```
hash_password(plain: str) -> str
```

_No description provided._

_Defined at line 99._

### `verify_password`

```
verify_password(plain: str, hashed: str) -> bool
```

_No description provided._

_Defined at line 103._

### `create_access_token`

```
create_access_token(user_id: int, role: str) -> str
```

_No description provided._

_Defined at line 110._

### `decode_token`

```
decode_token(token: str) -> dict
```

_No description provided._

_Defined at line 122._

### `get_user_by_email`

```
get_user_by_email(conn: sqlite3.Connection, email: str) -> dict | None
```

_No description provided._

_Defined at line 147._

### `get_user_by_id`

```
get_user_by_id(conn: sqlite3.Connection, user_id: int) -> dict | None
```

_No description provided._

_Defined at line 155._

### `create_user`

```
create_user(conn: sqlite3.Connection, *, email: str, name: str, role: str, password: str) -> dict
```

_No description provided._

_Defined at line 163._

### `seed_test_user`

```
seed_test_user(conn: sqlite3.Connection) -> None
```

Create the demo `test@aglex.ua` account if missing (Phase 2.1 step 3).

_Defined at line 187._

### `current_user`

```
current_user(token: Optional[str] = Depends(_oauth2_scheme), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

Resolve `Authorization: Bearer <token>` to a user row.

Raises 401 for missing, malformed, expired, or "user-no-longer-exists"
tokens. Drop this dep into any protected route: `Depends(current_user)`.

_Defined at line 212._

### `register`

```
@router.post('/register', response_model=TokenResponse, status_code=status.HTTP_201_CREATED)
```

```
register(req: RegisterRequest, conn: sqlite3.Connection = Depends(get_db)) -> TokenResponse
```

_No description provided._

_Defined at line 258._

### `login`

```
@router.post('/login', response_model=TokenResponse)
```

```
login(req: LoginRequest, conn: sqlite3.Connection = Depends(get_db)) -> TokenResponse
```

_No description provided._

_Defined at line 271._

### `me`

```
@router.get('/me', response_model=UserOut)
```

```
me(user: dict = Depends(current_user)) -> UserOut
```

_No description provided._

_Defined at line 285._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_to_bcrypt_input`

```
_to_bcrypt_input(plain: str) -> bytes
```

UTF-8 encode + truncate to bcrypt's 72-byte input limit.

_Defined at line 94._

### `_row_to_user`

```
_row_to_user(row: tuple | None) -> dict | None
```

_No description provided._

_Defined at line 134._

### `_credentials_exception`

```
_credentials_exception(detail: str = 'Could not validate credentials') -> HTTPException
```

_No description provided._

_Defined at line 204._

### `_user_out`

```
_user_out(user: dict) -> UserOut
```

_No description provided._

_Defined at line 247._
