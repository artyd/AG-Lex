<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/matters_routes.py`

_Python module_

## Overview

Phase 2.4 — custom router for /api/matters with row-level access control.

Replaces the generic CRUD router built by `build_router(MATTERS)`. Every
endpoint here goes through one of two dependencies:

- `current_user_text_id` — resolves the TEXT user id (`u1, u2, …`) the rest
  of the domain uses.
- `require_member("case_id")` — 403s when the authenticated user isn't in
  `case_members` for the case in the URL.

PATCH is field-level: each changed column writes a row to `activity_log` so
the matter timeline is the audit trail. Child endpoints
(`POST /api/matters/{id}/{notes|hearings|parties|tasks|time-entries}`) write
the child + an `activity_log` row + (Phase 3) broadcast to other members.

Code generation: `{TYPE_PREFIX}-{YEAR}-{NN}` where the NN bumps over the max
NN already taken by that prefix+year. SQLite `UNIQUE` on `matters.code`
plus a retry loop guards the race when two clients create matters of the
same type in the same second.

## Public API

### `MemberRef` · class

```
class MemberRef
```

_No description provided._

_Defined at line 130._

### `CaseCreate` · class

```
class CaseCreate
```

_No description provided._

_Defined at line 135._

### `CasePatch` · class

```
class CasePatch
```

_No description provided._

_Defined at line 152._

### `NoteCreate` · class

```
class NoteCreate
```

_No description provided._

_Defined at line 177._

### `HearingCreate` · class

```
class HearingCreate
```

_No description provided._

_Defined at line 181._

### `PartyCreate` · class

```
class PartyCreate
```

_No description provided._

_Defined at line 190._

### `TaskCreate` · class

```
class TaskCreate
```

_No description provided._

_Defined at line 197._

### `TimeEntryCreate` · class

```
class TimeEntryCreate
```

_No description provided._

_Defined at line 205._

### `list_matters`

```
@router.get('')
```

```
list_matters(user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> list[dict]
```

Only matters where the current user is in `case_members`.

_Defined at line 240._

### `get_matter`

```
@router.get('/{case_id}')
```

```
get_matter(case_id: str, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 357._

### `create_matter`

```
@router.post('', status_code=status.HTTP_201_CREATED)
```

```
create_matter(body: CaseCreate, user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 376._

### `patch_matter`

```
@router.patch('/{case_id}')
```

```
patch_matter(case_id: str, body: CasePatch, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 457._

### `add_member_route`

```
@router.post('/{case_id}/members', status_code=status.HTTP_201_CREATED)
```

```
add_member_route(case_id: str, body: MemberRef, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 529._

### `remove_member_route`

```
@router.delete('/{case_id}/members/{member_user_id}', status_code=status.HTTP_204_NO_CONTENT)
```

```
remove_member_route(case_id: str, member_user_id: str, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> None
```

_No description provided._

_Defined at line 551._

### `add_note`

```
@router.post('/{case_id}/notes', status_code=status.HTTP_201_CREATED)
```

```
add_note(case_id: str, body: NoteCreate, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 578._

### `add_hearing`

```
@router.post('/{case_id}/hearings', status_code=status.HTTP_201_CREATED)
```

```
add_hearing(case_id: str, body: HearingCreate, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 602._

### `add_party`

```
@router.post('/{case_id}/parties', status_code=status.HTTP_201_CREATED)
```

```
add_party(case_id: str, body: PartyCreate, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 629._

### `add_task`

```
@router.post('/{case_id}/tasks', status_code=status.HTTP_201_CREATED)
```

```
add_task(case_id: str, body: TaskCreate, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

Add a task linked to this case. Reuses the existing `tasks` table —
the `matter` column holds the matter code (legacy), and the id prefix
`tk-` lets the existing tasks router find this row too.

_Defined at line 652._

### `add_time_entry`

```
@router.post('/{case_id}/time-entries', status_code=status.HTTP_201_CREATED)
```

```
add_time_entry(case_id: str, body: TimeEntryCreate, user_text_id: str = Depends(_require_member), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 684._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_now`

```
_now() -> str
```

_No description provided._

_Defined at line 53._

### `_generate_code`

```
_generate_code(conn: sqlite3.Connection, matter_type: str) -> str
```

_No description provided._

_Defined at line 75._

### `_row_to_card`

```
_row_to_card(row: sqlite3.Row | tuple) -> dict
```

Trim shape for the list endpoint: just what MatterCard renders.

_Defined at line 115._

### `_require_member`

```
_require_member(case_id: str, conn: sqlite3.Connection = Depends(get_db), user_text_id: str = Depends(current_user_text_id)) -> str
```

Inline membership check returning the user_text_id so handlers
don't have to declare both dependencies.

_Defined at line 220._

### `_hydrate_case`

```
_hydrate_case(conn: sqlite3.Connection, case_id: str) -> dict | None
```

Fully hydrated case: row + members + parties + notes + hearings +
activity_log timeline.

_Defined at line 261._
