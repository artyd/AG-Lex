<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/mock_ai.py`

_Python module_

## Overview

Deterministic fixtures used when AGLEX_MOCK_AI=1.

Every AI entry point (`pipeline.analyze`, `contract_analysis.analyze_contract`,
`reconciliation.reconcile`) checks `is_mock_ai()` and returns the matching
fixture before touching the Claude SDK. Used by:

- e2e tests (Playwright) so flows are deterministic and don't burn tokens
- offline development when no API_KEY is configured

The fixtures are shape-stable with the real response so the frontend renders
identically; only the contents are canned. The `suggest.from` strings are
chosen to match the sample DOCX shipped in `e2e/fixtures/` — the highlight
mapper finds them verbatim and produces real <mark> overlays.

## Public API

### `is_mock_ai`

```
is_mock_ai() -> bool
```

_No description provided._

_Defined at line 22._

### `mock_display_pdf_bytes`

```
mock_display_pdf_bytes() -> bytes | None
```

_No description provided._

_Defined at line 30._

### `mock_contract_analysis`

```
mock_contract_analysis() -> dict[str, Any]
```

_No description provided._

_Defined at line 50._

### `mock_analyze_answer`

```
mock_analyze_answer(question: str) -> dict[str, Any]
```

_No description provided._

_Defined at line 146._

### `mock_reconciliation`

```
mock_reconciliation() -> dict[str, Any]
```

_No description provided._

_Defined at line 168._
