<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/main.py`

_Python module_

## Overview

AG Lex FastAPI app — single-origin: serves /api/* JSON and the Vite build at /.

API routes are all defined above the SPA static mount so they win the route
match against `/{full_path:path}`.

## Public API

### `lifespan` · async

```
@asynccontextmanager
```

```
lifespan(app: FastAPI)
```

Bring schemas up to date and seed the demo data on boot.

Idempotent: all DDL is `CREATE IF NOT EXISTS`, `seed_test_user` no-ops
when the row already exists, and `seed_all` uses INSERT OR IGNORE. Safe to
run on every restart.

_Defined at line 66._

### `create_contract_with_pdf`

```
@app.post('/api/contracts', status_code=201, dependencies=[Depends(current_user)], tags=['contracts'])
```

```
create_contract_with_pdf(body: _ContractCreatePayload, conn: sqlite3.Connection = Depends(get_db)) -> dict
```

_No description provided._

_Defined at line 142._

### `health`

```
@app.get('/health')
```

```
health() -> dict[str, str]
```

_No description provided._

_Defined at line 214._

### `realtime_endpoint` · async

```
@app.websocket('/ws')
```

```
realtime_endpoint(websocket: WebSocket, token: str = Query(..., description='JWT access token from /api/auth/login.'), conn: _sqlite3.Connection = Depends(get_db)) -> None
```

_No description provided._

_Defined at line 236._

### `upload_document` · async

```
@app.post('/api/upload', dependencies=[Depends(current_user)])
```

```
upload_document(file: UploadFile = File(...))
```

Ingest a contract: PDF or DOCX → Markdown + sections + token stats.

Pre-deploy audit: gated by `current_user` so anonymous callers can't burn
server CPU on PDF/DOCX conversion. No specific capability required —
uploading itself is read-flavored and grants no AI/billing access.

_Defined at line 276._

### `AnalyzeRequest` · class

```
class AnalyzeRequest
```

_No description provided._

_Defined at line 366._

### `codex_stats`

```
@app.get('/api/codex/stats', dependencies=[Depends(require('view'))])
```

```
codex_stats(conn: sqlite3.Connection = Depends(get_db)) -> dict
```

Fix 3: codex inventory + health probe.

Used by `scripts/check_codex.py` and by ops to confirm the RAG corpus is
loaded. Any authenticated user with the `view` capability can hit it —
that's everyone in the default permissions matrix.

_Defined at line 380._

### `analyze_endpoint`

```
@app.post('/api/analyze', dependencies=[Depends(require('ai'))])
```

```
analyze_endpoint(req: AnalyzeRequest)
```

RAG turn: search codex → ask Claude → validate citations.

Phase 2.3: gated by `ai` capability — controls who can burn Claude tokens.

_Defined at line 391._

### `ContractAnalysisRequest` · class

```
class ContractAnalysisRequest
```

_No description provided._

_Defined at line 407._

### `analyze_contract_endpoint`

```
@app.post('/api/analyze/contract', dependencies=[Depends(require('ai'))])
```

```
analyze_contract_endpoint(req: ContractAnalysisRequest, conn: sqlite3.Connection = Depends(get_db))
```

Phase 3.1: full per-contract analysis → findings/comparison/legal_basis/score.

Accepts either `markdown` or `sections`. Gated by the `ai` capability.

_Defined at line 438._

### `reconcile_endpoint` · async

```
@app.post('/api/reconcile', dependencies=[Depends(require('ai'))])
```

```
reconcile_endpoint(contract_file: UploadFile = File(...), handover_file: UploadFile = File(...), user: dict = Depends(current_user), conn: sqlite3.Connection = Depends(get_db)) -> dict
```

Contract ↔ Handover (Table 3) reconciliation.

Two-file multipart upload. Each file is parsed (PDF/DOCX/XLSX → Markdown)
and handed to Claude with the reconciliation schema. The result is
persisted to `reconciliations` so it shows up in Library/History.

_Defined at line 541._

### `contract_display_pdf`

```
@app.get('/api/contracts/{rid}/display.pdf', dependencies=[Depends(current_user)], include_in_schema=False)
```

```
contract_display_pdf(rid: str, conn: sqlite3.Connection = Depends(get_db))
```

_No description provided._

_Defined at line 709._

### `reconciliation_contract_display_pdf`

```
@app.get('/api/reconciliations/{rid}/contract-display.pdf', dependencies=[Depends(current_user)], include_in_schema=False)
```

```
reconciliation_contract_display_pdf(rid: str, conn: sqlite3.Connection = Depends(get_db))
```

_No description provided._

_Defined at line 720._

### `reconciliation_handover_display_pdf`

```
@app.get('/api/reconciliations/{rid}/handover-display.pdf', dependencies=[Depends(current_user)], include_in_schema=False)
```

```
reconciliation_handover_display_pdf(rid: str, conn: sqlite3.Connection = Depends(get_db))
```

_No description provided._

_Defined at line 734._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_ContractCreatePayload` · class

```
class _ContractCreatePayload
```

_No description provided._

_Defined at line 120._

### `_sections_to_text`

```
_sections_to_text(sections: list[dict]) -> str
```

Re-join Phase 1.3 sections into a markdown-like document for Claude.

_Defined at line 423._

### `_ingest_upload` · async

```
_ingest_upload(file: UploadFile, allowed: set[str], role: str) -> tuple[str, str, bytes | None, dict | None, str]
```

Reusable: validate + persist to a temp file + convert to MD, HTML, PDF.

Returns (markdown, html, display_pdf_bytes, display_pdf_error, original_filename).
Markdown feeds Claude; HTML is the source-side fallback; the display PDF
is what the FE renders pixel-perfect via PDF.js. The PDF is best-effort:
when soffice is missing or crashes we log, return None for the bytes, AND
populate display_pdf_error = {"kind": "...", "message": "..."} so the
caller can persist the reason — that lets the 404 from the display
endpoint tell the user *why* without SSH/journal access.

Raises HTTPException on any validation failure of the input file. The
temp file is deleted before returning.

_Defined at line 458._

### `_stream_display_pdf`

```
_stream_display_pdf(conn: sqlite3.Connection, table: str, blob_col: str, row_id: str, err_col: str | None = None) -> 'Response'
```

Stream the display PDF, or 404 with the saved soffice failure reason.

When `err_col` is provided and the BLOB is NULL, we pull the persisted
{"kind", "message"} from that column and return it in the 404 body as
`{detail, kind, message}` so the FE banner can explain the failure
without anyone reading server logs.

_Defined at line 655._
