<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/search.py`

_Python module_

## Overview

Hybrid codex search: vector (sqlite-vec) + keyword (FTS5) fused via RRF.

Three public entry points:

    search_by_vector(query, source=None, limit=5)
    search_by_text(query, source=None, limit=5)
    hybrid_search(query, source=None, limit=5)

Each returns dicts shaped `{id, article_number, title, content, source, score}`
where higher `score` means more relevant. `source` accepts either a string
(`"ЦКУ"`) or a list of strings (`["ЦКУ", "ГКУ"]`) — used to scope queries to
UA codices, EU regulations, or one specific source.

Vectors are produced by the same Sentence-Transformer model the importer used
(`settings.EMBED_MODEL`). Using a different model here would silently return
junk — Phase 1.1's pitfall, called out in the design doc.

## Public API

### `search_by_vector`

```
search_by_vector(query: str, source = None, limit: int = DEFAULT_LIMIT, *, conn: sqlite3.Connection | None = None, embedder = None) -> list[dict]
```

Cosine-nearest articles. Score = 1 - cosine_distance ∈ [-1, 1].

_Defined at line 105._

### `search_by_text`

```
search_by_text(query: str, source = None, limit: int = DEFAULT_LIMIT, *, conn: sqlite3.Connection | None = None) -> list[dict]
```

BM25 keyword search via FTS5. Score is positive (negated bm25).

_Defined at line 136._

### `hybrid_search`

```
hybrid_search(query: str, source = None, limit: int = DEFAULT_LIMIT, *, conn: sqlite3.Connection | None = None, embedder = None, k: int = RRF_K) -> list[dict]
```

Reciprocal rank fusion of `search_by_vector` + `search_by_text`.

Each search runs at `limit * 2` so the fusion has slack to combine ranks;
we then trim to `limit`. Documents present in both lists get the largest
boost, which is the point of hybrid retrieval.

_Defined at line 170._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_default_embedder`

```
@functools.lru_cache(maxsize=1)
```

```
_default_embedder()
```

_No description provided._

_Defined at line 40._

### `_encode_query`

```
_encode_query(query: str, embedder = None) -> bytes
```

_No description provided._

_Defined at line 45._

### `_source_clause`

```
_source_clause(source) -> tuple[str, list]
```

_No description provided._

_Defined at line 60._

### `_sanitize_fts`

```
_sanitize_fts(query: str) -> str
```

_No description provided._

_Defined at line 83._

### `_row_to_dict`

```
_row_to_dict(row: sqlite3.Row | tuple, score: float) -> dict
```

_No description provided._

_Defined at line 95._

### `_reciprocal_rank_fusion`

```
_reciprocal_rank_fusion(ranked_lists: Sequence[Iterable[dict]], k: int = RRF_K, limit: int = DEFAULT_LIMIT) -> list[dict]
```

_No description provided._

_Defined at line 202._
