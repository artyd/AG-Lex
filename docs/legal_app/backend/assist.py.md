<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `legal_app/backend/assist.py`

_Python module_

## Overview

Lawyer-assist endpoints: summary + translation (Phase 3.2).

Two thin Claude wrappers driving the two analysis-screen modals:

  POST /api/summary    → {summary, mode, usage, model}
  POST /api/translate  → {pairs, glossary, translation, direction, usage, model}

Both are gated by `require("ai")` per Phase 2.3 — the same gate that protects
`/api/analyze` and `/api/analyze/contract`. Errors from the SDK become a clean
`ClaudeError` and surface as HTTP 502 at the route layer.

Translation uses `output_config.format = json_schema` so paragraph pairs and
glossary survive structured. Summary is plain markdown — the modal renders
it directly.

## Public API

### `generate_summary`

```
generate_summary(text: str, mode: SummaryMode, *, client: anthropic.Anthropic | None = None, max_tokens: int = DEFAULT_SUMMARY_MAX_TOKENS) -> dict
```

Return Claude's markdown summary of the contract.

`mode` selects the prompt — `legal` for the lawyer summary, `plain` for the
client-facing explanation (which carries its own "not a legal opinion"
disclaimer in the prompt).

_Defined at line 140._

### `generate_translation`

```
generate_translation(text: str, direction: TranslateDirection, *, client: anthropic.Anthropic | None = None, max_tokens: int = DEFAULT_TRANSLATE_MAX_TOKENS) -> dict
```

Return paragraph-paired translation + glossary.

_Defined at line 185._

### `SummaryRequest` · class

```
class SummaryRequest
```

_No description provided._

_Defined at line 256._

### `summary`

```
@router.post('/summary', dependencies=[Depends(require('ai'))])
```

```
summary(req: SummaryRequest)
```

_No description provided._

_Defined at line 287._

### `TranslateRequest` · class

```
class TranslateRequest
```

_No description provided._

_Defined at line 297._

### `translate`

```
@router.post('/translate', dependencies=[Depends(require('ai'))])
```

```
translate(req: TranslateRequest)
```

_No description provided._

_Defined at line 306._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `_direction_label`

```
_direction_label(direction: TranslateDirection) -> str
```

_No description provided._

_Defined at line 90._

### `_sections_to_text`

```
_sections_to_text(sections: list[dict]) -> str
```

Re-join Phase 1.3 sections back into markdown for Claude.

Local copy of `main._sections_to_text` so this module stays import-free of
`main`. Behaviour is identical.

_Defined at line 94._

### `_wrap_anthropic_errors`

```
_wrap_anthropic_errors(call)
```

Decorator-ish helper — runs `call()` and maps SDK errors to ClaudeError.

_Defined at line 113._

### `_usage_dict`

```
_usage_dict(usage) -> dict
```

_No description provided._

_Defined at line 127._

### `_resolve_contract_text`

```
_resolve_contract_text(req_text_field: Optional[str], req_markdown: Optional[str], req_sections: Optional[list[dict]]) -> str
```

_No description provided._

_Defined at line 277._
