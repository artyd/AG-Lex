<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `src/ui/tweaks-panel.jsx`

_Javascript module_

## Overview

@ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)

## Public API

### `useTweaks`

```
export useTweaks(defaults)
```

── useTweaks ───────────────────────────────────────────────────────────────
Single source of truth for tweak values. setTweak persists via the host
(__edit_mode_set_keys → host rewrites the EDITMODE block on disk).

_Defined at line 174._

### `TweaksPanel`

```
export TweaksPanel({ title = 'Tweaks', children })
```

── TweaksPanel ─────────────────────────────────────────────────────────────
Floating shell. Registers the protocol listener BEFORE announcing
availability — if the announce ran first, the host's activate could land
before our handler exists and the toolbar toggle would silently no-op.
The close button posts __edit_mode_dismissed so the host's toolbar toggle
flips off in lockstep; the host echoes __deactivate_edit_mode back which
is what actually hides the panel.

_Defined at line 198._

### `TweakSection`

```
export TweakSection({ label, children })
```

── Layout helpers ──────────────────────────────────────────────────────────

_Defined at line 290._

### `TweakRow`

```
export TweakRow({ label, value, children, inline = false })
```

_No description provided._

_Defined at line 299._

### `TweakSlider`

```
export TweakSlider({ label, value, min = 0, max = 100, step = 1, unit = '', onChange })
```

── Controls ────────────────────────────────────────────────────────────────

_Defined at line 313._

### `TweakToggle`

```
export TweakToggle({ label, value, onChange })
```

_No description provided._

_Defined at line 322._

### `TweakRadio`

```
export TweakRadio({ label, value, options, onChange })
```

_No description provided._

_Defined at line 333._

### `TweakSelect`

```
export TweakSelect({ label, value, options, onChange })
```

_No description provided._

_Defined at line 405._

### `TweakText`

```
export TweakText({ label, value, placeholder, onChange })
```

_No description provided._

_Defined at line 419._

### `TweakNumber`

```
export TweakNumber({ label, value, min, max, step = 1, unit = '', onChange })
```

_No description provided._

_Defined at line 428._

### `TweakColor`

```
export TweakColor({ label, value, options, onChange })
```

TweakColor — curated color/palette picker. Each option is either a single
hex string or an array of 1-5 hex strings; the card adapts — a lone color
renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
rest stacked in a sharp column on the right. onChange emits the
option in the shape it was passed (string stays string, array stays array).
Without options it falls back to the native color input for back-compat.

_Defined at line 488._

### `TweakButton`

```
export TweakButton({ label, onClick, secondary = false })
```

_No description provided._

_Defined at line 531._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `onMsg`

```
onMsg(e)
```

_No description provided._

_Defined at line 231._

### `dismiss`

```
dismiss()
```

_No description provided._

_Defined at line 241._

### `onDragStart`

```
onDragStart(e)
```

_No description provided._

_Defined at line 246._

### `move`

```
move(ev)
```

_No description provided._

_Defined at line 253._

### `up`

```
up()
```

_No description provided._

_Defined at line 260._

### `labelLen`

```
labelLen(o)
```

Segments wrap mid-word once per-segment width runs out. The track is
~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
back to a dropdown rather than wrap.

_Defined at line 346._

### `resolve`

```
resolve(s)
```

<select> emits strings — map back to the original option value so the
fallback stays type-preserving (numbers, booleans) like the segment path.

_Defined at line 352._

### `segAt`

```
segAt(clientX)
```

_No description provided._

_Defined at line 363._

### `onPointerDown`

```
onPointerDown(e)
```

_No description provided._

_Defined at line 370._

### `move`

```
move(ev)
```

_No description provided._

_Defined at line 374._

### `up`

```
up()
```

_No description provided._

_Defined at line 379._

### `clamp`

```
clamp(n)
```

_No description provided._

_Defined at line 429._

### `onScrubStart`

```
onScrubStart(e)
```

_No description provided._

_Defined at line 435._

### `move`

```
move(ev)
```

_No description provided._

_Defined at line 439._

### `up`

```
up()
```

_No description provided._

_Defined at line 445._

### `__twkIsLight`

```
__twkIsLight(hex)
```

Relative-luminance contrast pick — checkmarks drawn over a swatch need to
read on both #111 and #fafafa without per-option configuration. Hex input
only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".

_Defined at line 465._

### `__TwkCheck`

```
__TwkCheck({ light })
```

_No description provided._

_Defined at line 474._

### `key`

```
key(o)
```

Native <input type=color> emits lowercase hex per the HTML spec, so
compare case-insensitively. String() guards JSON.stringify(undefined),
which returns the primitive undefined (no .toLowerCase).

_Defined at line 501._
