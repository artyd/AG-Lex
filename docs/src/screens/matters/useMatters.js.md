<!-- AUTOGENERATED by scripts/generate_docs.py — do not edit by hand. -->
<!-- Re-run `npm run docs` (or `python scripts/generate_docs.py`) after
     changing the function it describes. The pre-commit hook will block
     a commit that changes a function without regenerating its doc. -->

# `src/screens/matters/useMatters.js`

_Javascript module_

## Overview

============================================================
useMatters — list-level hook for /api/matters.
Loads from the server, subscribes to realtime case/member events
for incremental updates, refetches on reconnect.

The server-side shape is partially flattened (nextDeadline is a
bare date, members come back via the hydrated GET). `adaptCard`
normalises rows to the richer local shape the existing components
already render, so the UI layer doesn't have to know about the
server/local divergence.
============================================================

## Public API

### `adaptCard`

```
export adaptCard(row)
```

Server card → local card.

_Defined at line 18._

### `adaptDetail`

```
export adaptDetail(row)
```

Hydrated case from GET /api/matters/{id} → local detail shape.

_Defined at line 51._

### `useMatters`

```
export useMatters()
```

Hook: list of matters scoped to the current user (server-enforced).

_Defined at line 99._

### `useMatterDetail`

```
export useMatterDetail(caseId)
```

Hook: hydrated detail for one case, refreshed on every relevant event.

_Defined at line 138._

## Internal

_Helpers used inside this module. Document them so a later refactor doesn't lose the why._

### `deriveParties`

```
deriveParties(rows, fallbackClient)
```

_No description provided._

_Defined at line 84._

### `reload`

```
reload()
```

_No description provided._

_Defined at line 103._

### `reload`

```
reload()
```

_No description provided._

_Defined at line 142._

### `filter`

```
filter(ev)
```

_No description provided._

_Defined at line 154._
